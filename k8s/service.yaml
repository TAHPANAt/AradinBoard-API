apiVersion: v1
kind: Service
metadata:
  name: aradinboard-service
spec:
  type: NodePort
  selector:
    app: aradinboard
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080